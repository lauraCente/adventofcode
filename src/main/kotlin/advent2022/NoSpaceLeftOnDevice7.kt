package advent2022

import java.util.*
import kotlin.io.path.Path

fun main() {
    val scanner = Scanner(Path("src/main/resources/day7_t.txt"))

    while (scanner.hasNext()){
        scanner.nextLine().contains("cd")
    }
}